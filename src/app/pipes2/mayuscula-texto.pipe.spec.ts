import { MayusculaTextoPipe } from './mayuscula-texto.pipe';

describe('MayusculaTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new MayusculaTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
