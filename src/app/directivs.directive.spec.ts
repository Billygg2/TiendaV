import { DirectivsDirective } from './directivs.directive';

describe('DirectivsDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivsDirective();
    expect(directive).toBeTruthy();
  });
});
