import { ImagenVerDirective } from './imagen-ver.directive';

describe('ImagenVerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagenVerDirective();
    expect(directive).toBeTruthy();
  });
});
